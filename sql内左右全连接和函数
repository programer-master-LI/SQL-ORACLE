MySQL 支持哪些类型的连接（JOIN）
1. INNER JOIN（内连接）
返回两个表中匹配的记录。
SELECT * FROM Orders 
INNER JOIN Customers ON Orders.CustomerID = Customers.CustomerID;
2. LEFT JOIN（左连接）
返回左表所有记录，以及右表中匹配的记录。
SELECT *
FROM Customers
LEFT JOIN Orders
ON Customers.CustomerID = Orders.CustomerID;
3. RIGHT JOIN（右连接）
返回右表所有记录，以及左表中匹配的记录。
SELECT *
FROM Customers
RIGHT JOIN Orders
ON Customers.CustomerID = Orders.CustomerID;
4. CROSS JOIN（交叉连接）
返回两个表的笛卡尔积。
SELECT *
FROM Customers
CROSS JOIN Orders;
