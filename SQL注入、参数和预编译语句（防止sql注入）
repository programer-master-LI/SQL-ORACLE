SQL注入是一种 代码注入技术，可以破坏您的数据库。SQL注入是一种常见的 网络攻击技术。

SQL注入是指 攻击者将 恶意SQL代码插入 用户输入字段，从而 读取、修改或删除数据库中的敏感数据。

SQL 注入通常 发生在您要求用户输入（例如用户名/用户 ID）时，攻击者 不是提供名称/ID，而是插入一个 SQL 命令来 执行数据库中的某些操作。
请看以下示例，该示例 SELECT通过将变量 (txtUserId) 添加到 下拉字符串来创建语句。该变量取自用户输入 (getRequestString)：
txtUserId = getRequestString("UserId");
txtSQL = "SELECT * FROM Users WHERE UserId = " + txtUserId;   从users中选择所有id=+txtuserid时的记录

介绍防止 SQL 注入的最有效方法，即使用 SQL参数和 SQL预处理语句。
 SQL 注入攻击手段有：
1.基于 1=1 总是成立的的 SQL 注入
SQL 代码的最初目的是选择 具有给定用户 ID 的用户。
        如果没有措施阻止用户输入“错误”内容，用户可以输入一些“智能”内容，例如：
用户身份：
105 OR 1=1
那么，SQL语句将如下所示：
SELECT * FROM Users WHERE UserId = 105 OR 1=1;
上面的 SQL 语句是有效的，它将返回“Users”表中的所有行，因为 OR 1=1始终为 TRUE。

上面的例子看起来危险吗？如果“用户”表  包含用户名和密码呢？
///黑客只需在 输入字段中插入 105 OR 1=1，即可获取  数据库中的所有用户名和密码。

2.基于 “OR” "="始终为真的 SQL 注入   （原理）
以下是一个网站用户登录示例：
用户名：
John Doe
密码：
myPass
例子
uName = getRequestString("username");
uPass = getRequestString("userpassword");
sql = 'SELECT * FROM Users WHERE Name ="' + uName + '" AND Pass ="' + uPass + '"'
结果
SELECT * FROM Users WHERE Name ="John Doe" AND Pass ="myPass"
黑客只需在用户名或密码文本框中 插入 “或”or即可  获取数据库中的用户名和密码：
用户名：
" or ""="
密码：
" or ""="
SQL语句现在看起来像这样：
结果
SELECT * FROM Users WHERE Name ="" or ""="" AND Pass ="" or ""=""
上面的 SQL 语句是有效的，它将返回“Users”表中的 所有行，因为OR ""=""始终为 TRUE。

3.利用批量 SQL 语句进行 SQL 注入
批量 SQL 语句是将 两个或多个 SQL 语句用 分号分隔成一组。
以下 SQL 语句将返回“Users”表中的 所有行，然后 删除“Suppliers”表。
例子
SELECT * FROM Users; DROP TABLE Suppliers;
请看以下示例：
例子
txtUserId = getRequestString("UserId");
txtSQL = "SELECT * FROM Users WHERE UserId = " + txtUserId;
以及以下输入：
用户身份：105; DROP TABLE Suppliers
有效的 SQL 语句如下所示：
结果
SELECT * FROM Users WHERE UserId = 105; DROP TABLE Suppliers;

4.参数 - 防止SQL注入
SQL 参数（参数化查询）可用于 保护网站免受 SQL 注入攻击。
参数化查询 是一种 SQL 语句，它使用占位符 而不是直接 将输入值添加到查询文本中。
/查询执行时，占位符会 被实际值替换。这使得查询更安全、更易于重用。
大多数数据库都支持参数化查询，但语法各不相同：
MySQL 使用 ? 表示 参数
SQL Server 使用 @ 表示参数
PostgreSQL 使用 $ 表示参数   （刀乐）
SQL 参数是在 SQL 查询执行时以 可控的方式 添加到 SQL 查询中的。

ASP.NET Razor 示例
userid = getRequestString("UserId");
query = "SELECT * FROM Users WHERE UserId = @userid";
db.Execute(query, userid);
请注意，SQL Server 中的 参数用 @ 标记表示。
SQL 引擎会检查每个参数，以确保其与所属列匹配，并按字面意思处理，而不是作为要执行的 SQL 的一部分。
另一个例子
cname = getRequestString("CustomerName");
caddress = getRequestString("Address");
ccity = getRequestString("City");
query = "INSERT INTO Customers (CustomerName, Address, City) Values(@cname, @caddress, @ccity)";
db.Execute(query, cname, caddress, ccity);
示例
以下示例展示了如何在一些常见的   Web 语言中构建参数化查询。
ASP.NET 中的 SELECT 语句：
userid = getRequestString("UserId");
query = "SELECT * FROM Customers WHERE CustomerId = @userid";
cmd = new SqlCommand(query);
cmd.Parameters.AddWithValue("@userid", userid);
cmd.ExecuteReader();

5.预编译语句 - 防止SQL注入      （相当于在写一个sql脚本）
SQL预处理语句可用于 保护网站免受SQL注入攻击。
预编译语句将 查询结构（SQL）与  实际数据（用户输入）分离。
/预处理报表的基本工作原理如下：
准备：向服务器发送一个包含占位符的 SQL 查询模板。不发送数据值。
例如：INSERT INTO MyGuests VALUES(?, ?, ?)。然后，服务器解析、编译和优化该 SQL 查询模板，但不会执行它。
执行：稍后，应用程序将值绑定到参数，数据库执行查询。应用程序可以使用不同的值多次执行查询。
/预先编制的财务报表具有四大优势：
缩短解析时间——因为查询的准备工作只进行一次（尽管语句执行了多次）。
最小化带宽——绑定参数可以最小化服务器带宽，因为每次只需发送参数，而无需发送整个查询。
安全性——预处理语句对于抵御 SQL 注入攻击非常有效，因为稍后使用不同协议传输的参数值无需进行正确的转义。如果原始语句模板并非源自外部输入，则不会发生 SQL 注入攻击。
更简洁的代码——通过将数据与 SQL 命令分离

MySQL 中的预编译语句        (相当于一个脚本)
以下示例摘自《PHP MySQL 预处理语句》，并使用了 MySQL 中的预处理语句：
示例 - 使用预处理语句的 MySQL
<?php
$servername = "localhost";
$username = "username";
$password = "password";
$dbname = "myDB";
// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
// Check connection
if ($conn->connect_error) {
  die("Connection failed: " . $conn->connect_error);
}
// SQL query template
$sql = "INSERT INTO MyGuests (firstname, lastname, email) VALUES (?, ?, ?)";

// Prepare the SQL query template
if($stmt = $conn->prepare($sql)) {
  // Bind parameters
  $stmt->bind_param("sss", $firstname, $lastname, $email);
  // Set parameters and execute
  $firstname = "John";
  $lastname = "Doe";
  $email = "john@example.com";
  $stmt->execute();
  $firstname = "Mary";
  $lastname = "Moe";
  $email = "mary@example.com";
  $stmt->execute();
  $firstname = "Julie";
  $lastname = "Dooley";
  $email = "julie@example.com";
  $stmt->execute();
  echo "New records created successfully";
} else {
  echo "Error: " . $sql . "<br>" . $conn->error;
}
$stmt->close();
$conn->close();
?>
代码解释
在 SQL 语句中，问号 (?) 是  名字、 姓氏和电子邮件值的占位符： 
"INSERT INTO MyGuests (firstname, lastname, email) VALUES (?, ?, ?)"
现在，我们来看一下这个 bind_param()函数。这个函数将 变量绑定到 SQL 查询中的 占位符。
占位符（？）会在执行时被 变量中实际的值替换。
“sss”参数列出了 每个参数的数据类型。字符“s” 告诉 MySQL 该参数是字符串。
我们必须为每个参数定义一个这样的参数。
通过告诉 MySQL 预期的数据类型，我们可以最大限度地  降低 SQL 注入的风险：
$stmt->bind_param("sss", $firstname, $lastname, $email);
类型参数可以是以下四种类型之一：
i - 整数（正整数）
d - 双精度浮点数
s - 字符串（文本）
b - 二进制（图像、PDF 等）
注意：如果我们要插入来自外部来源的数据（例如用户输入），则对数据进行清理和验证非常重要。

6.托管hosting
如果您希望您的网站能够 存储和检索 数据库中的数据，那么您的 Web 服务器必须能够 访问数据库系统。
如果您的网络服务器由 互联网服务提供商 (ISP) 托管，您将需要寻找  SQL 托管方案。
一些常见的 SQL 托管数据库包括 MySQL、PostgreSQL、SQL Server 和 Oracle。
MySQL
MySQL 是一款流行的网站数据库软件。
MySQL 以速度快、可靠性高、易于使用而闻名。
MySQL 是微软和 Oracle 昂贵解决方案的一种廉价替代方案。

SQL Server
Microsoft SQL Server适用于 高流量的数据库驱动型网站。
Microsoft SQL Server 与其他 Microsoft 产品具有强大的集成性、强大的工具和可靠的安全功能。

甲骨文
Oracle 是一款适用于高流量数据库驱动型网站的流行数据库软件。
Oracle 以其极高的可扩展性、性能、可靠性和安全性而闻名。
Oracle的授权费用昂贵。

PostgreSQL
PostgreSQL 是一款流行的网站数据库软件。
PostgreSQL 具有先进的功能、高标准的合规性、可扩展性和强大的数据完整性。
PostgreSQL是开源且免费的。









