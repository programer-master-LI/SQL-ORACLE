数据处理————高级查询————数据库管理

SELECT 列名 FROM 表名 是 SQL 查询数据的标准语法。
EXTRACT 用于提取日期时间字段，不用于选择列。
SELECT FirstName FROM Persons;
语句正式结束时要加封号
文本字段与数字字段
SQL 要求文本值   用单引号括起来（大多数数据库系统也允许使用双引号）。
但是，数值字段   不应该用引号括起来：SELECT * FROM Customers WHERE CustomerID = 1;

1.在 MySQL 中，如何从名为 "Persons" 的表中选择所有列
SELECT * FROM Persons;                            (所有列）
* 表示选择表中的所有列。

SELECT column1, column2, ...FROM table_name;     （两列多列）

2.在MySQL表格中，一列可能包含多个重复值——有时你只想列出唯一值。哪个语句只返回不同的值（不重复）重复的值只显示一次。
SELECT DISTINCT column1，column2  FROM table_name;      （表中选择所有不同的值） SELECT DISTINCT Country FROM Customers;
DISTINCT 用于去除重复值，只返回唯一值。

统计不同值select COUNT(DISTINCT column列名  )
通过使用 COUNT()带有 DISTINCT关键字的函数，我们可以统计不同国家的数量。
SELECT COUNT(DISTINCT Country) FROM Customers;     (计数器count)

3.如何从名为“Persons”的表中选择  “FirstName”  列的值为“Peter”的所有记录
   使用 SQL 的 WHERE 子句表示选择所有仅提取满足特定条件的记录。筛选记录
SELECT * FROM Persons WHERE FirstName = 'Peter';   (where 条件句)
SELECT *：表示选择表中的所有列。
FROM Persons：指定数据来源表为 Persons。
WHERE FirstName = 'Peter'：筛选出 FirstName 等于 Peter 的记录。
SELECT column1, column2, ...FROM table_name WHERE condition;

WHERE 子句中的运算符
除了运算符=><，>=<=，<>，之外，您还可以使用其他运算符 =来筛选搜索结果。
BETWEEN，LIKE，IN
             列名=某值表示条件，还可以用判断句表条件

4.如何从名为“Persons”的表中选择所有“FirstName”  列的值以“a”开头的记录
使用 SQL 的 LIKE 运算符
SELECT * FROM Persons WHERE FirstName LIKE 'a%';     （筛选LIKE 运算符）  LIKE 'a%'之间没有=号，like自己就是运算符     
LIKE：用于模式匹配。                      常用来筛选以字母a,b,c开头的单词
'a%'：
a 表示以字母 a 开头；
% 表示 后面可以跟任意数量任意长的字符。即a后面跟很多字符，Alice，anna
      数据库采用不区分大小写的排序规则（MySQL 默认常见情况）

所有“LastName”  字母顺序 在“Hansen”和“Pettersen”之间的记录（包括“Hansen”和“Pettersen”）。h i j k l m n o p
从表中选择所有 LastName 按字母

5.如何从名为“Persons”的表中选择所有“FirstName”为“Peter” 且“LastName”为“Jackson”的记录
or表示只满足一个条件就选出来，and表示前后都满足才选出来
WHERE子句可以包含一个或   多个 AND运算符，用于根据多个条件筛选记录。
可以包含一个或多个OR运算符，用于根据多个条件筛选记录。
SELECT *
FROM Persons
WHERE FirstName = 'Peter'
AND LastName = 'Jackson';                              （and or运算符）

还可以  组合AND使用OR运算符。
SELECT * FROM Customers
WHERE Country = 'Spain' AND (CustomerName LIKE 'G%' OR CustomerName LIKE 'R%');
如果不加括号，上面的 SQL 语句将返回所有来自西班牙且以“G”开头的客户，以及 所有以“R”开头的客户，无论其国家/地区值如何：
SELECT * FROM Customers
WHERE Country = 'Spain' AND CustomerName LIKE 'G%' OR CustomerName LIKE 'R%';就会不受约束了前一个条件，挑选任何国家R开头的客户


6.如何从名为“Persons”的表中选择顺序位于 "Hansen" 和 "Pettersen" 之间（包含两端值）的记录，可以使用 BETWEEN。
SELECT *
FROM Persons
WHERE LastName BETWEEN 'Hansen' AND 'Pettersen';           （BETWEEN运算符）
   BETWEEN A AND B 表示：>= A AND <= B                
包含边界值，因此：
Hansen 会被选中
Pettersen 也会被选中
等价写法
SELECT *
FROM Persons
WHERE LastName >= 'Hansen' AND LastName <= 'Pettersen';

7.非运算符NOT运算符用于子句中WHERE ，返回所有不符合指定条件的记录。它将条件的结果从真反转为假，反之亦然。
SELECT * FROM Customers WHERE NOT Country = 'Spain';
NOT运算符与运算符结合使用 =。NOT运算符还可以与其他运算符结合使用，以排除数据，例如：
NOT LIKE
NOT BETWEEN
NOT IN
IS NOT NULL        
NOT EXISTS

NOT LIKE运算符用于子句中WHERE，以排除与指定字符模式匹配的行。
通常有两个通配符与运算符一起使用NOT LIKE：
百分号% - 表示零个、一个或多个字符
下划线符号_- 代表一个字符
以下 SQL 语句    选择所有不以字母“A”开头的客户：
SELECT * FROM Customers WHERE CustomerName NOT LIKE 'A%';

NOT BETWEEN运算符用于子句中WHERE，以选择值超出指定包含范围的行。
该NOT BETWEEN运算符可用于数值、文本或日期值。
以下 SQL 语句选择 CustomerID 不在 10 到 60 之间的所有客户：
SELECT * FROM Customers WHERE CustomerID NOT BETWEEN 10 AND 60;

NOT IN运算符用于WHERE 子句中，以排除与指定列表或子查询结果集中的任何值匹配的行。
以下 SQL 语句选择所有 City 不在“Paris”或“London”中的客户：
SELECT * FROM Customers WHERE City NOT IN ('Paris', 'London');

不大于SQL 中，“非大于”条件最常用的是标准大于等于 (>=) 运算符。
以下 SQL 语句选择 CustomerID 不大于 50 的所有客户：
SELECT * FROM Customers WHERE NOT CustomerID > 50;

不小于SQL 中，“不小于”条件最常用的是小于等于 (<=) 运算符。
以下 SQL 语句选择 CustomerID 不小于 50 的所有客户：
SELECT * FROM Customers WHERE NOT CustomerId < 50;

IS NOT NULL    （语法列名 IS NOT NULL）
判断字段值不为空，专门用来检测列的值是否不是 NULL。
注意：NULL ≠ 空字符串 ''、≠ 0。SQL 中 NULL 和任何值比较结果都是未知，必须用 IS NULL / IS NOT NULL
null它本身并不是一个值，而是一个占位符，用于指示数据缺失。有NULL 值的字段是指在创建记录时留空的字段。
SELECT * FROM student WHERE name IS NOT NULL;
只返回了id=1 的数据（id=3 的 name 是 NULL，会被过滤）。
SELECT column_names FROM table_name WHERE column_name IS NULL(IS NOT NULL);

NOT EXISTS判断子查询没有返回任何结果    （不存在 匹配的数据）。
常用于关联两张表，判断「主表记录  在 子表中无对应关联数据」。
WHERE NOT EXISTS (子查询)
EXISTS / NOT EXISTS 只判断有无结果集，不关心返回什么字段，子查询写 SELECT * 效率最高；
执行逻辑： 外层表 逐行匹配 子查询，大数据量下效率通常优于 NOT IN
两张表：
student(id, name) 学生表
score(stu_id, subject) 成绩表
需求：查询 没有任何成绩 的学生
SELECT * FROM student s
WHERE NOT EXISTS (
    SELECT * FROM score sc
    WHERE sc.stu_id = s.id
);
逻辑：
对每一条学生记录，检查成绩表中不存在对应学号 → 选出无成绩的学生。

8.使用哪个关键字对结果集进行排序                       ORDER BY
ORDER BY关键字用于按升序或降序对结果集进行排序。
SELECT column1, column2, ...FROM table_name
ORDER BY column1, column2, ... ASC|DESC;
默认情况下，该ORDER BY关键字按升序（ASC）对结果集进行排序。
按 LastName 升序排序：
SELECT *FROM Persons ORDER BY LastName（ASC）;      (ORDER BY按升降序对结果集进行排序)

按 Age 降序排序：
SELECT *FROM Persons ORDER BY Age DESC;
排序方式
ASC：升序（默认）
DESC：降序

SELECT * FROM Customers ORDER BY Country ASC, CustomerName DESC;

字符串值升降序
SELECT * FROM Products ORDER BY ProductName;    按productname升序排列
//对于字符串值productname，该ORDER BY会按列名column 字母顺序 对列中的值进行排序：
//要按降序排列列中的文本值，请使用DESC关键字

按多列排序
SELECT * FROM Customers ORDER BY Country, CustomerName;    
意味着它 首先按国家/地区排序，如果某些记录的国家/地区相同，则 按客户名称排序
SELECT * FROM Customers ORDER BY Country ASC, CustomerName DESC;      按“Country”列升序排序，按“CustomerName”列降序排序

9.如何向“Persons”表中插入一条新记录，使用 INSERT INTO 语句        
    语法INSERT INTO 表名 (列1, 列2, ...) VALUES (值1, 值2, ...); （ 插入列和值，INSERT INTO 语句）
INSERT INTO Persons (FirstName, LastName, Age) VALUES ('John', 'Smith', 25);
  如果为 所有列提供值 可以省略列名：但是，这些值的顺序必须与表格中列的顺序一致：
INSERT INTO Persons VALUES (1, 'John', 'Smith', 25);            （插入单 行记录）

如何使用 MySQL 将“Olsen”作为“LastName”插入到“Persons”表中
如果只给 LastName 列赋值，可以使用：
INSERT INTO Persons (LastName) VALUES ('Olsen');                （只在表格的某些特定指定列中插入值）
INSERT INTO Persons：向 Persons 表插入数据。
(LastName)：指定插入的列。VALUES ('Olsen')：插入的值。

插入多行
要插入多行数据，我们使用相同的INSERT INTO语句，但使用多个值，      用逗号分隔每组值,
以下 SQL 语句在“Customers”表中插入三条新记录：
INSERT INTO Customers (CustomerName, ContactName, Address, City, PostalCode, Country)
VALUES
('Cardinal', 'Tom B. Erichsen', 'Skagen 21', 'Stavanger', '4006', 'Norway'),   逗号
('Greasy Burger', 'Per Olsen', 'Gateveien 15', 'Sandnes', '4306', 'Norway'),   逗号
('Tasty Tee', 'Finn Egan', 'Streetroad 19B', 'Liverpool', 'L1 0AA', 'UK');

10.如何将 Persons 表中“LastName”列中的“Hansen”更改为“Nilsen”（替换）对应增删查改中的改，更新
   UPDATE语句用于更新或修改表中的一条或多条记录。
UPDATE table_name SET column1 = value1, column2 = value2 WHERE condition;
语句 WHERE该WHERE子句指定要更新哪些记录。如果省略该WHERE子句，则表中所有记录都将被更新！
UPDATE Persons SET LastName = 'Nilsen' WHERE LastName = 'Hansen';      (更新替换记录)
说明UPDATE Persons：更新 Persons 表。
SET LastName = 'Nilsen'：将 LastName 修改为 Nilsen。
         WHERE LastName = 'Hansen'：只修改原来为 Hansen 的记录。where指定某某处的记录

//更新多条记录
//以下 SQL 语句会将国家/地区为“墨西哥”的所有记录的 ContactName 更新为“Juan”：
//UPDATE Customers SET ContactName='Juan' WHERE Country='Mexico';

11.如何删除 Persons 表中“FirstName”为“Peter”的记录
DELETE FROM Persons WHERE FirstName = 'Peter';             (删除表中已有的记录)
DELETE FROM Persons：从 Persons 表中删除记录。
WHERE FirstName = 'Peter'：只删除名字为 Peter 的记录。
      如果省略 WHERE子句，DELETE FROM Persons;删除表中的所有记录。

删除所有记录
可以在不删除表的情况下 删除表中的所有记录。这意味着表的结构、属性和索引都将保持不变。
DELETE FROM table_name;
以下 SQL 语句会删除“Customers”表中的所有记录，而不会删除该表：
DELETE FROM Customers;

删除表格
要彻底删除整个表，使用以下DROP TABLE语句：
DROP TABLE table_name;

12.SELECT TOP 子句   （mysql支持两种写法）
SELECT TOP子句用于 限制要返回的记录数。
SELECT TOP子句适用于 包含数千条记录的大型表。返回大量记录可能会  影响性能。
1.以下 SQL 语句仅选择“Customers”表中的前 3 条记录：                 ///从表中所有记录选///
SELECT （TOP 3） * FROM Customers;                              （限制返回记录条数）
SELECT * FROM Customers （LIMIT 3）;     等效示例
MySQL 支持使用该LIMIT子句来选择有限数量的记录，而 Oracle 使用. FETCH FIRST n ROWS ONLY
SELECT * FROM Customers FETCH FIRST 3 ROWS ONLY;

SQL Server/MS Access 的语法
SELECT TOP number|percent column_name(s) FROM table_name WHERE condition;
MySQL 语法
SELECT column_name(s) FROM table_name WHERE condition LIMIT number;
Oracle 12+ 的语法
SELECT column_name(s) FROM table_name ORDER BY column_name(s) FETCH FIRST number ROWS ONLY;

2.高阶用法SQL TOP PERCENT 
使用SELECT TOP带有百分比语法的子句。从“Customers”表中选择前 50% 的记录适用于 SQL Server，
SELECT TOP 50 PERCENT * FROM Customers;  
展示 Oracle 的等效示例：
SELECT * FROM Customers FETCH FIRST 50 PERCENT ROWS ONLY;
                                                 
3.选择前几个结果，并带 WHERE 条件                               
从“Customers”表中选择前三条记录，其中 Country 为“Germany”适用于 SQL Server，
SELECT TOP 3 * FROM Customers WHERE Country = 'Germany';                 ///从指定记录中选///
 MySQL 的等效示例：
SELECT * FROM Customers WHERE Country = 'Germany' LIMIT 3;
Oracle 的等效示例：
SELECT * FROM Customers WHERE Country = 'Germany' FETCH FIRST 3 ROWS ONLY;

4.SELECT TOP and ORDER BY
ORDER BY当需要  对结果进行排序时，添加关键字，并返回  排序结果的前 3 条记录。
适用于 SQL Server
按 CustomerName 的字母顺序对结果进行反向排序，并返回前 3 条记录：
SELECT TOP 3 * FROM Customers ORDER BY CustomerName DESC;
 MySQL 的等效示例：
SELECT * FROM Customers ORDER BY CustomerName DESC LIMIT 3;
Oracle 的等效示例：
SELECT * FROM Customers ORDER BY CustomerName DESC FETCH FIRST 3 ROWS ONLY;







如何返回“Persons”表中的记录数        SELECT COUNT（列名） FROM 表
SELECT COUNT(*) FROM Persons;
COUNT(*)：统计表中的所有记录（行）数。计数器
FROM Persons：从 Persons 表中统计。
返回：
COUNT(*)
3
也可以统计某一列的 非空值数量：
SELECT COUNT(LastName) FROM Persons;




哪个运算符用于选择给定范围内的值    BETWEEN 
选择年龄在 18 到 30 之间的记录（包含 18 和 30）：
SELECT *
FROM Persons
WHERE Age BETWEEN 18 AND 30;

NOT NULL 约束强制列不接受 NULL 值,确保某列不能存储 NULL 值
如
CREATE TABLE Persons (
    ID INT NOT NULL,
    FirstName VARCHAR(50) NOT NULL,
    LastName VARCHAR(50)
);
其中：
ID INT NOT NULL
FirstName VARCHAR(50) NOT NULL
表示：
ID 不能为空
FirstName 不能为空

下面的插入会失败：
INSERT INTO Persons (ID, FirstName)
VALUES (NULL, 'Peter');
因为 ID 列设置了 NOT NULL。

哪个运算符用于在列中搜索指定的模式    LIKE
查找所有 FirstName 以字母 A 开头的记录：
SELECT *
FROM Persons
WHERE FirstName LIKE 'A%';

WHERE FirstName LIKE '_a%'
表示第二个字母是 a。
%	任意数量的字符，，_	单个字符

使用哪个 MySQL 语句可以创建名为“Customers”的数据库表？
CREATE TABLE 表名 (
    列名1 数据类型,
    列名2 数据类型,
    ...
);
CREATE TABLE Customers (
    CustomerID INT NOT NULL,       数据类型 和 规定不空值
    CustomerName VARCHAR(100),      VARCHAR = Variable Character（可变长度字符串）规定100 = 最多可存储 100 个字符
    ContactName VARCHAR(100),
    City VARCHAR(50)
);








